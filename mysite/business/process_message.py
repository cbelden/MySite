def submit_message(input):
    # Extract message information
    name = input['name']
    number = input['number']
    email = input['email']
    message = input['message']

    # Construct email

    # Do anthing else
